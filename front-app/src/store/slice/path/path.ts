import { createSlice } from '@reduxjs/toolkit'

export const PathSlice = createSlice({
  name: 'Path',
  initialState: {
    Path: 'https://servidordepaginagp-production.up.railway.app/'
  },
  reducers: {
    
  },
})

export default PathSlice.reducer