medidas numericas
	para una muestra   --> estadisticas
	para una poblacion --> parametros

medidas de centralizacion:
	la media el promedio de un conjunto de n mediciones es igual a la suma de las mediciones dividida entre n.
	~
	x --> indica la muestra de una muestra = la sumatoria de las mediciones / cantidad de mediciones
	y(con el parlito para el otro lado) --> indica la muestra de una poblacion
	
	mediana(me) --> cae en la POSICION DEL MEDIO cuando se ordena de mayor a menor
		    --> (n + 1) / 2 --> esta saca la posicion del la medicion
		    --> esta se saca de la siguiente manera:
		    	buscamos el siguiente de donde esta si tenemos 4 y 13 y buscamos 10 agarramos el 13(esto en frecuencia acomulada)
			luego agarramos el linite inferior (15-17 agarramos el 15), luego buscamos la sumatoria de numero de datos
			y la dividimos en 2 , luego agarramos el frecuencia anterio anterior y se la restmos el calculo anterio , a este resultado
			lo dividimos por la frecuencia del intervalo actual, y luego a este lo multiplicamos por la amplitud del intervalo , 
			en este caso 2 (15-17) , a este le sumos el primero numero que buscamos el limite nferior y chanta

			me = Li + (N/2 - Fi-1)/fi * a



	moda(mo) --> medicion con mas frecuencua

medidas de dispercion:
	la variabilidad o dispersion
	sirve para tener una imagen mental de la dispersion de los datos

	rango (R) = xmax - xmin

	la varianza de una poblacion de n mediciones es el promedio de los cuadrdos de las desviacione de las mediciones lrededor de su media ~x
	variaza = o~**2 = 
	poblacion:		
	    ~				    
	    v ** 2 = la suma de las x - promedio de x**2 / n
		
	    	   = (E ((x - ~x)**2))/n
	muestra:
	    ~
	    s **2 = la suma de las x - promedio de x **2 / n - 1
:
	    	  = (E((x - ~x)**2))/n-1

	desvio estandar raiz cuadrada positica de la varianza
	~    ~
	v = (v**2)**(1/2)




resumen de sinbologia:
	xi = x
	n = N --> en la mayoria de los casos
	media = ~
		x --> muestra
		y --> poblacion
	mediana = (me)
	moda = (mo)
	rango = (R)
			     ~
	varianza (poblacion):v**2
	varianza (muestra):  s**2
	desvio estandar: ~
			 v

medidas de posicion:
	cuartiles: 100/4
	q1 = 0 - 25
	q2 = 0 - 50
	....

	deciles: 100/10
	percentiles: 100/100

	todos se sacan con:
		numerp de percentil * n / numero relativo de la medida (4,10,100)




